# 引き継ぎログ

複数のセッションが同じリポジトリを並行して触っています。**誰が何を変えたかを1行ずつ残す**ためのファイルです。

## なぜ要るか

2026-08-20 に「アプリのドア・プロフィール文・コメント返信」を同じ日に動かして、何が効いたか分からなくなった前例があります。同じことが「別々のセッションが良かれと思って別々に直す」形でも起きます。

実際 2026-08-25 の作業中、別セッションのコミット3件が main に入っていることに偶然気づきました。

## ルール

1. **変更を main に入れたら、下の表に1行足す。**PR番号・日付・何を変えたか
2. **測定に影響する変更は、同じ日に2つ以上入れない。**入れると効果が切り分けられない
3. **数字を出したら、試行回数も書く。**「偽陽性6%」だけでは判断できない

---

## 触ってはいけない線

- **`MIN_DAYS = 21` を下げない。**短くすると偽陽性を出すアプリになり、このサイトが記事で否定してきたものになる
- **素朴な `p < 0.05` で拾わない。**候補数×部位×lag で1,000回超の検定になるため、ランダムデータからでも毎回「発見」が出る。並べ替えで作った p 値を使い、**必ず合成データで偽陽性率を測ってから**入れる
- **記録は端末内のみ（localStorage + IndexedDB）。**入力内容を1バイトも外に出さない。信用の核
- **「肌が良くなる」「原因はこれ」と初日に言わない。**返せるのは事実だけ

### 分析を変えるときの必須手順

1. 因果ゼロの合成データを流して**誤報率**を測る（**150試行以上**）
2. 原因を仕込んだデータで**検出力**を測る（同上）
3. **誤報率を揃えて**から検出力を比べる。揃えずに比べると結論が逆に出る（2026-08-25 に実際にやらかした）

---

## ログ

| 日付 | PR | 何を変えたか |
|---|---|---|
| 08-15 | — | 肌カルテを独自ドメイン `hadakarte.com` に分離 |
| 08-18 | #1 | フェイスラインのゾーンが他と同じように色づくよう修正 |
| 08-19 | #2 | からだに「性行為」を追加／**21日未満だと構造的に1件も出せなかった問題**を修正（帰無分布に時間反転を追加）／記事のSEO |
| 08-19 | #3 | 記事 `/nannichi/` 公開／**本文リンクとCTAボタンが両テーマで基準割れ**していたのを修正 |
| 08-19 | #4 | `skipgc` を GA4 にも効かせ、運営側のアクセスを計測から除外 |
| 08-20 | — | 記事のCTAを「ほかの記事」の前に移動（nannichi, basho） |
| 08-20 | — | **本番ドメイン以外で GA4/GoatCounter を無効化**（それ以前は GA4 表示回数の約26%が localhost だった） |
| 08-20 | — | トップの見出しと初日体験を変更 |
| 08-25 | #5 | **21〜60日の判定を maxT から BH法に変更**＋それを支えるためブロック・ブートストラップの帰無分布を追加。61日以降は従来の maxT のまま。誤報率を変えずに検出力が 30日 5.3%→8.7%、60日 11.3%→25%（各50〜200試行） |
| 08-28 | #6 | **「記録が保存された」イベントを1日1回だけ計測。**内容・何日目・部位はいずれも送らない。ポリシー第4条に追記 |
| 08-28 | — | 記事 `/nannichi/` の更新日が実態とずれていたのを修正（8/20表記→8/25） |
| 08-28 | #8 | **`grid`（記録グリッド表示）イベントを追加**し、訪問→到達→保存の3段にした／初日体験の小改修3点（積み上げを主役に・ヒートマップを7日枠から広げる・初日に不足を告げない） |

---

## 保留中（マージしていない）

**項目3の .ics（カレンダーの繰り返し予定）** — ブランチ `ics-reminder` に実装済み・ローカル検証済み。

**保留の理由：iOS で動くか誰も確認できない。** 運営側に iPhone が無く、検証環境もローカルサーバーのみ。流入の87%がiOSなので、動かなければ効果はほぼゼロになる。

実装側で打てる手は打ってある（TZID を付けず現地時刻／スクリプト発火ではなくユーザー自身がタップするリンク）。**iPhone を1台借りて30秒試せば済む**ので、機会があれば確認してからマージする。動かなければ data URI 方式などに作り替える。

## いま進行中のこと（2026-08-28 時点）

**[定着の実行計画] の項目1（計測）まで完了。** 2〜4は未着手です。

- 🔴 **数日ベースラインを取る。**目安は **8/31以降かつ訪問100以上**。訪問が少ないと分母が足りず、記録開始率が読めない
- ベースラインが取れたら **2 → 3 → 4 の順に1つずつ**。同じ日に複数動かさない
- **項目2（プロフィールのリンク先を記事に）は測定対象から外す。**自己紹介リンクのタップが累計1では前後を比べようがない。やる理由は数字ではなく整合性（リールで「肝臓じゃない」と言った続きが読める場所に着地させる）。優先度は3・4より下

### 計測イベント（現在2つ。増やすときは慎重に）

| path | 意味 | 1日1回のキー |
|---|---|---|
| `grid` | 記録グリッドが画面の上6割に入った＝入力欄まで到達した | `hadaLog.ev.grid` |
| `save` | 記録が保存された | `hadaLog.ev`（既存キー） |

- **`記録タブ表示` にしなかった理由**：`v-log` は `class="view on"` で訪問時点から表示されているため、素直に実装すると訪問数と同じ数になり、段にならない。実際に越える必要があるのは紹介カードを読み飛ばして入力欄まで到達するところなので、そこで数えている
- **IntersectionObserver は使っていない**。描画しない環境では一度も発火せず動作を確認できなかったため、位置を直接見る方式にした
- 送るのは path / title / event のみ。**内容・日数・部位・数値は送らない**

### ベースラインから差し引く数

- **`save` イベント 1件**（2026-08-28、動作確認のため意図的に送信）
- 同日、検証中に除外フラグを一度消してしまい、**運営側のページビューが数件**混ざっています
